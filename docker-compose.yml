services:
  django:
    build:
      context: .
      dockerfile: ./docker/django/Dockerfile
    depends_on:
      - postgres
    env_file: ./.env
    environment:
      - READ_ENV_TEMPLATE=False
    restart: unless-stopped

  postgres:
    image: postgres:14
    env_file: ./.env
    restart: unless-stopped
