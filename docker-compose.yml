services:
  django:
    build:
      context: .
      dockerfile: ./docker/django/Dockerfile
    depends_on:
      - postgres
    env_file: ./.env
    environment:
      - READ_ENV_EXAMPLE=False
    networks:
      - django-postgres
  postgres:
    image: postgres:16
    env_file: ./.env
    networks:
      - django-postgres
networks:
  django-postgres:
